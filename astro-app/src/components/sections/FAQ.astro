---
import Container from "@/components/site/Container.astro";
import SectionHeading from "@/components/site/SectionHeading.astro";
import { site } from "@/lib/site";
---

<section id="faq" class="border-t border-border/60">
  <Container class="py-16 sm:py-20">
    <SectionHeading
      eyebrow="FAQ"
      title="Questions business owners usually ask"
      description="If you have a different question, just message me."
      align="center"
    />

    <div class="mt-10 grid gap-4">
      {site.faqs.map((item) => (
        <details class="group rounded-2xl border border-border bg-card p-6 shadow-sm">
          <summary class="cursor-pointer list-none text-sm font-semibold">
            <span class="flex items-center justify-between gap-4">
              <span>{item.q}</span>
              <span class="text-muted-foreground transition-transform group-open:rotate-45">
                +
              </span>
            </span>
          </summary>
          <p class="mt-3 text-sm text-muted-foreground">{item.a}</p>
        </details>
      ))}
    </div>
  </Container>
</section>

