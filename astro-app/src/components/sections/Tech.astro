---
import Container from "@/components/site/Container.astro";
import SectionHeading from "@/components/site/SectionHeading.astro";
import { site } from "@/lib/site";
---

<section id="tech" class="border-t border-border/60">
  <Container class="py-16 sm:py-20">
    <SectionHeading
      eyebrow="Tech Stack"
      title="Modern frameworks, but you don’t have to think about them"
      description="I use tools that produce fast, stable, and maintainable sites."
    />

    <div class="mt-10 flex flex-wrap gap-3">
      {site.frameworks.map((fw) => (
        <span class="inline-flex items-center rounded-full border border-border bg-card px-4 py-2 text-sm">
          {fw}
        </span>
      ))}
    </div>

    <div class="mt-8 grid gap-6 lg:grid-cols-3">
      <div class="rounded-2xl border border-border bg-card p-6 shadow-sm">
        <p class="text-sm font-semibold">Fast Loading</p>
        <p class="mt-2 text-sm text-muted-foreground">
          Better user experience and more inquiries from visitors.
        </p>
      </div>
      <div class="rounded-2xl border border-border bg-card p-6 shadow-sm">
        <p class="text-sm font-semibold">Easy to Update</p>
        <p class="mt-2 text-sm text-muted-foreground">
          Clean structure makes future edits and new pages simple.
        </p>
      </div>
      <div class="rounded-2xl border border-border bg-card p-6 shadow-sm">
        <p class="text-sm font-semibold">Production-Ready</p>
        <p class="mt-2 text-sm text-muted-foreground">
          SEO basics, responsive layout, and best practices by default.
        </p>
      </div>
    </div>
  </Container>
</section>
